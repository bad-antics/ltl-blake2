// examples/demo.ltl — runnable demo for ltl_blake2

import std::io
use ltl_blake2 as lib

fn main() {
    io::println("ltl_blake2 demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}